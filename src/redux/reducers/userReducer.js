const initialState = {
  name: 'Тестовый Стор',
  position: 'Тестировщик',
};

export default function userReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}